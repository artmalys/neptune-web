import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { Button } from '@transferwise/components';
import code from '../../../liveEditorCode/button.code';

Simple wrapper component for all button types and their states.

<LiveEditorBlock code={code} scope={{ Button }} />
<GeneratePropsTable componentName="Button" />

## Variants

<div className="scroll-table">
  <table className="docs-table table ">
    <thead>
      <tr>
        <th>Type</th>
        <th>Priority</th>
        <th>Preview</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <th>Accent</th>
        <th>Primary</th>
        <td>
          <Button block={true}>Continue</Button>
        </td>
      </tr>
      <tr>
        <th>Accent</th>
        <th>Secondary</th>
        <td>
          <Button priority="secondary" block={true}>
            Convert money
          </Button>
        </td>
      </tr>
      <tr>
        <th>Accent</th>
        <th>Tertiary</th>
        <td>
          <Button priority="tertiary" block={true}>
            Convert money
          </Button>
        </td>
      </tr>
      <tr>
        <th>Positive</th>
        <th>Primary</th>
        <td>
          <Button type="positive" block={true}>
            Send money
          </Button>
        </td>
      </tr>
      <tr>
        <th>Positive</th>
        <th>Secondary</th>
        <td>
          <Button type="positive" priority="secondary" block={true}>
            Confirm
          </Button>
        </td>
      </tr>
      <tr>
        <th>Negative</th>
        <th>Primary</th>
        <td>
          <Button type="negative" block={true}>
            Delete
          </Button>
        </td>
      </tr> <tr>
        <th>Negative</th>
        <th>Secondary</th>
        <td>
          <Button type="negative" priority="secondary" block={true}>
            Cancel
          </Button>
        </td>
      </tr>
    </tbody>
  </table>
</div>

export const meta = {
  name: 'Button',
};
